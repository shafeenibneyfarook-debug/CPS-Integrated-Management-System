# get link from terminal commands
cd frontend
npm install (first time only)
npm run dev


# git commit push commands
git init

git add .

git commit -m "Initial commit"

git branch -M main

git remote add origin https://github.com/shafeenibneyfarook-debug/CPS-Integrated-Management-System.git

git push -u origin main
